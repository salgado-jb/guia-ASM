#include "Memoria.h"

/*
int32_t strCmp(char* a, char* b){
    if (*a == '\0' && *b == '\0'){
        return 0;
    }
    else if (*a < *b){
        return -1;
    }
    else if (*a > *b){
        return 1;
    }
    else {
        strCmp(a+1, b+1);
    }
}
*/

/* Pueden programar alguna rutina auxiliar acá */
